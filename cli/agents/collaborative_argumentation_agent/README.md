# Collaborative Argumentation Agent

## Setup

1. Replace `AGENT_JWT` in `src/agent.py` with your valid token.
2. Install dependencies:
    pip install -r requirements.txt
3. Run the agent:
    python src/agent.py

## Description

Multi-agent framework to structure IT project argumentation via GenAI Session.